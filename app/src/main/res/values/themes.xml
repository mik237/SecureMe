<resources>
    <!-- Base application theme. -->
    <style name="Theme.SecureMe" parent="android:Theme.DeviceDefault.NoActionBar" />

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_navy</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SecureMe</item>
    </style>
</resources>
